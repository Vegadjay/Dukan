<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Showcase</title>
    <link rel="icon" type="image/x-icon" href="/Logo.webp">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.9.1/font/bootstrap-icons.min.css"
        rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">
    <style>
        :root {
            --primary-color: #7048e8;
            --secondary-color: #5f3dc4;
            --accent-color: #6741d9;
            --background-color: #f8f9fa;
            --order-btn-color: #00b4d8;
            --order-btn-hover: #0096c7;
            --logout-color: #e03131;
            --logout-hover: #c92a2a;
            --card-shadow: rgba(0, 0, 0, 0.1) 0px 10px 30px;
        }

        body {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            font-family: 'Poppins', sans-serif;
            line-height: 1.6;
            min-height: 100vh;
        }

        .navbar {
            background: rgba(255, 255, 255, 0.95) !important;
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
        }

        .navbar-brand img {
            border-radius: 12px;
            transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .navbar-brand img:hover {
            transform: scale(1.1) rotate(5deg);
        }

        .product-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 25px;
            padding: 30px 0;
        }

        .card {
            border: none;
            border-radius: 16px;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: var(--card-shadow);
            background: white;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: rgba(0, 0, 0, 0.15) 0px 20px 40px;
        }

        .card-img-top {
            height: 300px;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .card:hover .card-img-top {
            transform: scale(1.1);
        }

        .card-body {
            padding: 1.5rem;
            background: linear-gradient(180deg, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 1) 15%);
        }

        .card-title {
            font-weight: 600;
            color: #343a40;
            margin-bottom: 0.75rem;
            font-size: 1.25rem;
        }

        .card-text {
            color: var(--primary-color);
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 1.25rem;
        }

        .btn-group {
            display: flex;
            gap: 10px;
        }

        .btn-shop {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 10px;
            font-weight: 500;
            flex: 2;
            transition: all 0.3s ease;
        }

        .btn-shop:hover {
            background: var(--secondary-color);
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(112, 72, 232, 0.2);
            color: white;
        }

        .btn-details {
            background: #f8f9fa;
            color: #495057;
            border: 1px solid #dee2e6;
            padding: 10px 20px;
            border-radius: 10px;
            font-weight: 500;
            flex: 1;
            transition: all 0.3s ease;
        }

        .btn-details:hover {
            background: #e9ecef;
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.05);
        }

        .order-btn {
            background: var(--order-btn-color);
            color: white;
            border-radius: 10px;
            padding: 10px 25px;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
            margin-right: 15px;
        }

        .order-btn:hover {
            background: var(--order-btn-hover);
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 180, 216, 0.2);
            color: white;
        }

        .logout-btn {
            background: var(--logout-color);
            color: white;
            border-radius: 10px;
            padding: 10px 25px;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .logout-btn:hover {
            background: var(--logout-hover);
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(224, 49, 49, 0.2);
            color: white;
        }

        .no-products-container {
            background: white;
            border-radius: 16px;
            padding: 4rem 2rem;
            text-align: center;
            box-shadow: var(--card-shadow);
        }

        .no-products-message {
            font-size: 1.75rem;
            color: var(--secondary-color);
            margin-bottom: 1rem;
            font-weight: 600;
        }

        @media (max-width: 768px) {
            .product-grid {
                grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
                gap: 20px;
                padding: 20px 15px;
            }

            .card-img-top {
                height: 250px;
            }

            .order-btn,
            .logout-btn {
                margin: 5px;
                width: 100%;
                text-align: center;
            }

            .navbar-nav {
                margin-top: 1rem;
            }
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light sticky-top">
        <div class="container-fluid px-4">
            <a class="navbar-brand" href="#">
                <img src="/Logo.webp" alt="Logo" height="45" class="shadow-sm">
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <a href="/products/userorder" class="order-btn">
                    <i class="bi bi-bag-check-fill me-2"></i>My Orders
                </a>
                <a href="/api/auth/logout" class="logout-btn">
                    <i class="bi bi-box-arrow-right me-2"></i>Logout
                </a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="product-grid">
            <% if (products.length> 0) { %>
                <% products.forEach(function(product) { %>
                    <div class="card h-100">
                        <img src="data:image/jpeg;base64,<%= product.image.toString('base64') %>"
                            alt="<%= product.name %>" class="card-img-top">
                        <div class="card-body">
                            <div>
                                <h5 class="card-title">
                                    <%= product.name %>
                                </h5>
                                <p class="card-text">₹ <%= product.price.toLocaleString() %>
                                </p>
                            </div>
                            <div class="btn-group">
                                <a href="/products/payment/<%= product._id %>" class="btn btn-shop">
                                    <i class="bi bi-cart-fill me-2"></i>Shop Now
                                </a>
                                <a href="/products/productdetails/<%= product._id %>" class="btn btn-details">
                                    <i class="bi bi-info-circle me-1"></i>Details
                                </a>
                            </div>
                        </div>
                    </div>
                    <% }); %>
                        <% } else { %>
                            <div class="col-12">
                                <div class="no-products-container">
                                    <h2 class="no-products-message">
                                        <i class="bi bi-box-seam me-3"></i>No Products Available
                                    </h2>
                                    <p class="text-muted">Looks like there are no products in the inventory right now.
                                    </p>
                                </div>
                            </div>
                            <% } %>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>