<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - SnapShop</title>
    <link rel="icon" type="image/x-icon" href="/Logo.webp">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #007bff;
            --secondary-color: #6c757d;
            --gradient-start: #f4f5f7;
            --gradient-end: #e9ecef;
        }

        body {
            background: linear-gradient(135deg, var(--gradient-start), var(--gradient-end));
            background-size: 400% 400%;
            animation: gradientFlow 15s ease infinite;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        @keyframes gradientFlow {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        .login-container {
            max-width: 450px;
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .login-container:hover {
            transform: scale(1.02);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
        }

        .brand-logo {
            max-width: 120px;
            margin-bottom: 20px;
            animation: fadeIn 1s;
            filter: drop-shadow(0 4px 6px rgba(0, 0, 0, 0.1));
        }

        .brand-highlight {
            color: var(--primary-color);
            font-weight: 700;
        }

        .login-btn {
            position: relative;
            padding: 12px 20px;
            font-weight: 600;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
        }

        .login-btn i {
            margin-right: 10px;
        }

        .login-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }

        .customer-btn {
            background-color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .owner-btn {
            background-color: #28a745;
            border-color: #28a745;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.9);
            }

            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .alert {
            border-radius: 8px;
            animation: slideIn 0.5s ease;
        }

        @keyframes slideIn {
            from {
                transform: translateY(-20px);
                opacity: 0;
            }

            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .navbar {
            background-color: var(--primary-color);
        }

        .navbar-brand {
            font-weight: bold;
            color: white;
        }

        .footer {
            background-color: #f8f9fa;
            padding: 10px 0;
            text-align: center;
            font-size: 14px;
        }
    </style>
</head>

<body class="d-flex flex-column vh-100">

    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <img src="/Logo.webp" alt="" height="100px" width="100px" class="rounded-circle">
            <a class="navbar-brand" class="h1">SnapShop</a>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="flex-grow-1 d-flex align-items-center justify-content-center">
        <div class="login-container text-center p-5 shadow-lg">
            <img src="/Logo.webp" alt="SnapShop Logo" class="brand-logo">

            <h2 class="mb-2">Welcome to <span class="brand-highlight">SnapShop</span></h2>
            <p class="text-muted mb-4">Your one-stop platform for all your shopping needs</p>

            <!-- Flash Messages -->
            <% if (successMessage && successMessage.length> 0) { %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <%= successMessage[0] %>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
                <% } %>
                    <% if (errorMessage && errorMessage.length> 0) { %>
                        <div class="alert alert-danger alert-dismissible fade show" role="alert">
                            <%= errorMessage[0] %>
                                <button type="button" class="btn-close" data-bs-dismiss="alert"
                                    aria-label="Close"></button>
                        </div>
                        <% } %>
                            <!-- End Flash Messages -->

                            <h3 class="mb-4">Login Options</h3>
                            <div class="d-grid gap-3">
                                <a href="/auth/pages/users/loginpage"
                                    class="btn btn-primary login-btn customer-btn btn-lg w-100">
                                    <i class="fas fa-user"></i>Login as Customer
                                </a>
                                <a href="/auth/pages/owner/loginpage"
                                    class="btn btn-success login-btn owner-btn btn-lg w-100">
                                    <i class="fas fa-store"></i>Login as Shop Owner
                                </a>
                            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <p>&copy; 2024 SnapShop. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.querySelectorAll('.login-btn').forEach(btn => {
            btn.addEventListener('mouseenter', () => {
                btn.style.transform = 'translateY(-4px)';
            });
            btn.addEventListener('mouseleave', () => {
                btn.style.transform = 'translateY(0)';
            });
        });
    </script>
</body>

</html>